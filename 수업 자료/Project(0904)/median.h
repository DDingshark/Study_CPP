#ifndef GUARD_median_h //GUARD_median_h 가 정의되어있는지 확인 ->정의되어있지 않으면 실행 
#define GUARD_median_h// GUARD_median_h 정의 -> 전처리 변수(preprocessor variable)

#include<vector>
double median(std::vector<double> vec);


#endif //요기까지 -> 두번 include하는것을 방지 